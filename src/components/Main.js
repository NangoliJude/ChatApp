import React from 'react';
import { SectionWrapper } from './UI';

const Main = ()=>{
    return(
        <SectionWrapper>
              main
        </SectionWrapper>
        )
};

export default Main;